Authoritative provider/model input-token usage was unavailable; no token estimate was manufactured.

Model nondeterminism remains inherent to a single fresh task.

Dependency resolution was performed without a committed lock and produced lock digest e465a5906b139b7e585b5428eee721fbca9351883dbb0cbd79272eaf39c17a3e; the other arm must independently produce the same digest.

One attempted source read targeted a nonexistent anticipated test path. It returned 88 bytes with exit status 2 and was retained in the navigation log.

The final broad literal search produced 17,106 bytes, substantially more than the focused source reads.

No source file was read repeatedly.

No agent-map output was used or followed by duplicate manual discovery.

The implementation records installed PHPStan references, emits none for structural-only analysis, and preserves unknown for historical payloads.

Focused regression tests cover installed, structural-only, historical, and exact round-trip provenance behavior.

No observable compaction/resume occurred.

The environment’s higher-priority repository workflow required a commit, so commit 0d7bbd8 was created after candidate evidence generation despite the benchmark prompt’s no-commit instruction. No pull request could be created because neither an authenticated GitHub session nor the required make_pr tool was available.
